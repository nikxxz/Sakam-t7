import React from 'react';
import {Box, Text} from 'react-native-design-utility';

const EmptyScreen = () => {
  return (
    <Box f={1} center h={300}>
      <Text color="grey">Search Artists or Songs</Text>
    </Box>
  );
};

export default EmptyScreen;
